platform :ios, '9.0'
use_frameworks!
inhibit_all_warnings!

def available_pods
    pod 'Alamofire', '~> 4.4'
    pod 'AlamofireImage', '~> 3.2'
    pod 'SwiftyJSON'
end

target 'mobCommerce' do
    available_pods
end
