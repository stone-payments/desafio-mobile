<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/cartList_image"
        android:paddingRight="15dp"
        android:paddingTop="10dp"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/cartList_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="12dp"
        android:layout_marginStart="12dp"
        android:layout_toEndOf="@+id/cartList_image"
        android:layout_toRightOf="@+id/cartList_image"
        android:textSize="20dp"
        android:textColor="#000000"
        android:text="Título" />


    <Spinner
        android:id="@+id/spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:entries="@array/amountOptions"
        android:layout_alignBottom="@+id/cartList_image"
        android:layout_toRightOf="@+id/cartList_image"
        android:layout_toEndOf="@+id/cartList_image" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/cartList_price"
        android:layout_toRightOf="@id/spinner"
        android:textColor="#000000"
        android:layout_alignParentEnd ="true"
        android:layout_alignParentRight="true"
        android:layout_alignBottom="@id/spinner"
        android:text="R$ 999"
        android:layout_alignTop="@+id/spinner"
        android:textAlignment="textEnd"
        android:gravity="end"
        android:layout_marginRight="20dp"
        />

</RelativeLayout>